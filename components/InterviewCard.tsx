import React from 'react'

const InterviewCard = ({id,role,type,userId,techstack,createdAt}:InterviewCardProps) => {
    const feedback= null as Feedback | null;
  return (
    <div>
      
    </div>
  )
}

export default InterviewCard
